import { LogPublisherConfig } from './log-publisher-config';

describe('LogPublisherConfig', () => {
  it('should create an instance', () => {
    expect(new LogPublisherConfig()).toBeTruthy();
  });
});
